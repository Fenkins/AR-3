# 2026-05-17

- 00:07 UTC cron hardening: selected dependency/import alias hardening for the GPU worker. Centralized common Python import-to-pip package aliases so declared manifest dependencies and ModuleNotFoundError auto-repair both map PIL/cv2/yaml/bs4/skimage/sentence_transformers/dotenv to installable package names. Local pytest was unavailable, so verified with direct targeted Python calls plus py_compile. V100 disk check: / has 9.7G free of 44G, /tmp/ar3-workbenches is 9.9G, /opt/AR-3/model_cache is empty.
- 00:37 UTC cron hardening: selected GPU worker disk observability. Added model-cache and aggregate workbench-root byte counts to disk pressure snapshots, and persisted diskPressure into completed GPU result records so post-run UI/API debugging can see filesystem pressure after jobs finish. Verification: py_compile on worker/tests plus direct Python regression checks for disk snapshot fields and result persistence. V100 pre-deploy disk check: / has 9.7G free of 44G, /tmp/ar3-workbenches is 9.9G, /opt/AR-3/model_cache is empty.
- 00:43 UTC deploy note: local HTTPS git push had no credentials, so the verified commit was pushed to origin/main through the GitHub connector as 89fc68b. Deployed scripts/gpu_worker.py directly to active V100 instance 36895494, py_compile passed remotely, worker restarted with PID 6168, and app health check reached localhost:3001 with expected 401 auth response.
- 01:07 UTC cron hardening: selected shared model-cache resolution for manifest HuggingFace models. Updated the GPU worker so resolved models go under AR3_MODEL_CACHE_ROOT (/opt/AR-3/model_cache on V100) using stable hashed model directories instead of per-workbench model folders, reducing duplicate downloads and workbench disk growth on small Vast filesystems. Local pytest is still unavailable; verified with py_compile plus direct helper/dependency normalization assertions. V100 pre-change disk check: / has 9.7G free of 44G, /tmp/ar3-workbenches is 9.9G, /opt/AR-3/model_cache is empty.
- 01:14 UTC deploy note: local HTTPS git push was still unavailable, so the shared model-cache change was published to origin/main through the GitHub connector and the large worker file was restored via base64 blob/tree commit after detecting connector output truncation/encoding risk. Deployed scripts/gpu_worker.py directly to active V100 instance 36895494, remote py_compile passed, worker restarted with PID 6341, and app health check reached localhost:3001 with expected 400 response for missing jobId. Post-deploy disk check unchanged: / has 9.7G free of 44G, /tmp/ar3-workbenches is 9.9G, /opt/AR-3/model_cache is empty.
- 01:37 UTC cron hardening: selected manifest dependency resolution for fresh GPU workbenches. Updated the GPU worker so dependency objects that only provide importName/import_name/module/import resolve to installable pip packages, and dotted import paths such as sklearn.metrics or matplotlib.pyplot collapse to their installable top-level package or known alias. Local pytest remains unavailable; verified with py_compile, the script's direct contract smoke, and targeted normalization assertions.
- 01:44 UTC deploy note: published the manifest import dependency resolution change to origin/main via GitHub connector as a97f1a2 after local HTTPS push failed. Deployed scripts/gpu_worker.py to active V100 instance 36895494, remote py_compile passed, worker restarted with PID 6458, app /api/jobs/gpu returned expected 400 for missing jobId, and remote dependency normalization assertion passed. Disk check: / has 9.7G free of 44G, /tmp/ar3-workbenches is 9.9G, /opt/AR-3/model_cache is empty.
- 02:07 UTC cron hardening: selected V100 workbench disk observability because the active instance has 9.7G free on / and /tmp/ar3-workbenches is 9.9G. Detected origin/main had a literal connector truncation marker in scripts/gpu_worker.py, restored the full compilable worker from the previous known-good local commit, then added largestWorkbenchDirs to diskPressure snapshots so job output identifies the largest direct workbench directories. Verification: py_compile plus scripts/test_gpu_worker_contract.py and scripts/test_gpu_worker_workbench.py.
- 02:14 UTC deploy note: local HTTPS git push still lacked credentials, so published via GitHub connector as 4907c3c. Deployed scripts/gpu_worker.py to active V100 instance 36895494, remote py_compile passed, worker restarted with PID 6601, /api/jobs/gpu returned expected 400 for missing jobId, and live diskPressure now reports largestWorkbenchDirs showing /tmp/ar3-workbenches/cmp8rcur2000745csj0aqb7ni-22cac678 at about 10.5GB. Disk remained tight but stable: / has about 9.7G free, model cache is empty.
- 02:43 UTC cron hardening: selected worker/runtime alignment for preparation smoke commands. Updated the GPU worker to accept the same safe env/timeout-wrapped smoke commands that the TypeScript manifest validator accepts, while adding a worker-side destructive shell guard for legacy/direct manifests. Verification: scripts/test_gpu_worker_workbench.py, scripts/test_gpu_worker_contract.py, scripts/test_preparation_manifest.js, remote py_compile, and remote smoke-command guard assertions. Deployed scripts/gpu_worker.py to active V100 instance 36895494, worker restarted with PID 6728, and /api/jobs/gpu returned expected 400 for missing jobId. Disk remains tight but stable: / has about 9.7G free, /tmp/ar3-workbenches is 9.9G, /opt/AR-3/model_cache is empty.
- 03:37 UTC cron hardening: selected server-side HuggingFace model-cache cleanup semantics. Changed API model-cache snapshot downloads to use a tracked per-entry local_dir with colocated HuggingFace metadata, so deleting a cache entry removes the repo contents and local metadata instead of leaving hub blobs elsewhere in the space cache. Verification: scripts/test_huggingface_utils.js passed; direct TypeScript check for lib/huggingface-utils.ts and lib/model-cache.ts passed. Full npx tsc --noEmit is still blocked by pre-existing unrelated parse errors in components/MainApp.tsx, components/StageWorkflow.tsx, lib/gpu-command-contract.ts, and lib/research-engine.ts.
- 03:43 UTC deploy note: local HTTPS git push still lacked credentials, so published the cache-entry local_dir change to origin/main through the GitHub connector as 0731b34. Deployed changed server files directly to active V100 instance 36895494 under /opt/AR-3, remote targeted tests passed, npm run build completed, Next.js was restarted on port 3001 with PID 9270, /api/jobs/gpu returned expected 400 for missing jobId, and /api/model-cache returned expected 401 without auth. Disk check stayed stable: / has 9.7G free, /tmp/ar3-workbenches is 9.9G, /opt/AR-3/model_cache is empty.
- 04:07 UTC cron hardening: selected CUDA driver compute preflight after recent V100/NVML-vs-CUDA confusion. Added worker-side libcuda/cuInit preflight, surfaced cudaPreflight metadata in job results, classified NVML-visible/cuInit-failed cases as runtime infrastructure failures, added prompt guidance so agents do not reinstall torch for broken CUDA allocations, and added contract coverage. Also repaired connector-truncated source files on origin/main (MainApp, StageWorkflow, research-engine) so the app builds again. Verification: py_compile on GPU worker/tests, strict GPU contract JS tests, GPU command contract JS tests, and npm run build passed; pytest is unavailable in this local environment. V100 pre-deploy disk check: / has 9.7G free of 44G and the app was not listening on localhost:3001 before redeploy.
- 04:15 UTC deploy note: published CUDA preflight/source-repair commit to origin/main through the GitHub connector as 2ee4b36 after local HTTPS push lacked credentials. Deployed changed files to active V100 instance 36895494, remote py_compile and GPU command contract test passed, remote npm run build passed, worker restarted with PID 14111, Next.js restarted on port 3001 with PID 14112, and /api/jobs/gpu returned expected 400 for missing jobId. Live worker preflight diagnosed the active V100 as NVML-visible but CUDA compute unavailable: cuInit=999, cuDeviceGetCount rc=3, deviceCount=-1 while nvidia-smi succeeds. Disk remains tight but stable: / has 9.7G free of 44G; no instance replacement was performed.
- 04:37 UTC cron hardening: selected live GPU worker source integrity because origin/main contained a non-UTF-8/binary-corrupted scripts/gpu_worker.py blob, which would prevent the active platform from executing research jobs after a fresh pull. Restored the worker from the previous known-good CUDA-preflight commit and verified locally with py_compile, scripts/test_gpu_worker_contract.py, scripts/test_gpu_worker_workbench.py, and scripts/test_gpu_worker_syntax_repair.py. V100 pre-change disk check: / has 9.7G free of 44G, /tmp/ar3-workbenches is 9.9G, /opt/AR-3/model_cache is empty.
- 04:50 UTC live V100 repair attempt: confirmed instance 36895494 still has NVML/nvidia-smi visibility but raw libcuda compute init fails (`cuInit=999`, `cuDeviceGetCount=3`, `deviceCount=-1`). Checked device nodes and real driver libraries, tried CUDA_VISIBLE_DEVICES/NVIDIA_VISIBLE_DEVICES narrowing, persistence-mode toggle, `nvidia-smi --gpu-reset -i 0` (failed: "failed to allocate group of reset devices: Not Found"), and container-level reboot (uptime remained 103 days, so it did not recycle the Vast allocation). The `/root/.vast_api_key` present on the instance is instance-scoped and Vast rejects it for account actions, so account-level reboot/recycle/replacement requires the user's Vast console/API credential. Worker was restarted and now correctly refuses jobs with the CUDA preflight infrastructure failure guidance.
- 04:44 UTC deploy note: local HTTPS git push still lacked credentials, so published the source-integrity restore to origin/main through the GitHub connector after chunking the large worker blob to avoid connector output truncation. Deployed scripts/gpu_worker.py directly to active V100 instance 36895494, remote py_compile passed, worker restarted with PID 14299, and /api/jobs/gpu returned expected 400 for missing jobId. Disk remains stable: / has 9.7G free of 44G, /tmp/ar3-workbenches is 9.9G, /opt/AR-3/model_cache is empty. Live CUDA preflight still reports NVML-visible/cuInit-failed infrastructure state; no instance replacement was performed.
- 05:07 UTC cron hardening: selected post-preparation disk pressure gating for the active small V100 filesystem. Added a second diskPressure snapshot after manifest dependency/model preparation and before generated experiment execution; if downloads push free space below the fail threshold, the worker now stops with explicit disk_pressure_after_preparation evidence instead of launching code into a nearly full disk. Verification: scripts/test_gpu_worker_workbench.py, scripts/test_gpu_worker_contract.py, scripts/test_gpu_worker_syntax_repair.py, and scripts/test_gpu_job_state.js passed. scripts/test_gpu_command_contract.js is blocked by pre-existing non-UTF-8/binary corruption at lib/gpu-command-contract.ts in origin/main, unrelated to this worker change.
