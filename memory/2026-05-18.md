# 2026-05-18

- 12:17 UTC researcher hardening: audited the GPU researcher/implementer handoff for weak-model failures. The existing flow already generated dynamic preparation manifests, installed declared dependencies, prepared persistent per-space workbenches, and rejected prose/pseudocode before GPU execution, but the Python worker evidence gate did not enforce the validated preparation manifest's expected evidence and success thresholds at runtime. Added worker-side manifest criteria enforcement so Implementation jobs must now print concrete JSON fields satisfying dynamically discovered smoke-test expected evidence and numeric success thresholds; otherwise the job is classified as validation failure and can be retried instead of being recorded as a fake success. Attached the preparation manifest to worker validation, added failure classification markers, and extended isolated evidence-gate tests for missing expected metrics, failed thresholds, and passing concrete metrics. Verification: `python3 scripts/test_gpu_evidence_gate.py`, `python3 scripts/test_gpu_worker_contract.py`, `python3 scripts/test_gpu_worker_workbench.py`, and `python3 scripts/test_gpu_worker_syntax_repair.py` passed locally.
- 12:38 UTC Vast deployment: confirmed there were zero existing Vast instances before launch, then rented one AR-3 instance only: contract 37005780, GTX 1080 Ti 11GB, displayed burn about $0.0606/hr after disk/fees, SSH proxy ssh2.vast.ai:15780. Deployed origin/main commit c3a8c7a under /opt/AR-3, created a local SQLite `.env`, ran Prisma generate/db push and `npm run build`, then started search_service.py, gpu_worker.py, and Next.js on port 3001. CUDA preflight passed on the instance (`cuInit=0`, one CUDA device); worker reports NVIDIA GeForce GTX 1080 Ti, driver 570.158.01, 11264 MiB. Public quick tunnel is https://overall-retro-ohio-spending.trycloudflare.com and `/api/jobs/gpu` returns expected 400 for missing jobId. Also fixed deploy/vast-ai-launch-v3.py so fresh repository deployments create `.env` before Prisma commands.
